# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.*

# Testing
coverage

.vscode

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist
*.tsbuildinfo

# =====================================
# Terraform (Global)
# =====================================

# Terraform working directories
infra/**/.terraform/

# Terraform state files (NEVER commit)
terraform.tfstate
terraform.tfstate.*

# =====================================
# Localstack-specific (dev only)
# =====================================

# Localstack terraform lockfile (dev-only, noisy)
infra/localstack/.terraform.lock.hcl

# Localstack state backup files
infra/localstack/*.backup

# =====================================
# Production Terraform
# =====================================

# Production terraform lockfile SHOULD be committed
# (do NOT ignore infra/prod/.terraform.lock.hcl)

# Production state should be remote (S3 backend)
# so no local tfstate should exist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem